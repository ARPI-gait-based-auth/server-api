import sys

userName = sys.argv[1]
recordCsv = sys.argv[2]

print "Doe's CSV " + recordCsv[0:10] + "... belong to " + userName + "? (can we auth based on CSV?)"

print "unknown 87%"

sys.stdout.flush()
